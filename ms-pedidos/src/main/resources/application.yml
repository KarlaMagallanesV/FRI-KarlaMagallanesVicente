server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: ms-pedidos
  r2dbc:
    url: r2dbc:postgresql://${DB_HOST:ep-wandering-flower-atr9nncx-pooler.c-9.us-east-1.aws.neon.tech}/${DB_NAME:pedidos}?sslMode=${DB_SSL_MODE:require}
    username: ${DB_USER:neondb_owner}
    password: ${DB_PASSWORD:npg_tIe3BA1RvkJo}
    pool:
      enabled: true
      initial-size: 1
      max-size: 5
      max-idle-time: 10m
      validation-query: SELECT 1

servicios:
  productos-url: ${PRODUCTOS_SERVICE_URL:http://localhost:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
